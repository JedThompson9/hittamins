/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Local /public images work by default.
    // Add external domains here if product images are hosted remotely later.
    remotePatterns: [],
  },
}

module.exports = nextConfig
